Please refactor my WESAD PyTorch project so that repeated preprocessing, training, evaluation, and artifact-saving functions are moved out of the notebooks and imported from a small reusable package.

## Current files

My project currently contains these notebooks:

```text
notebooks/
├── 00_data_exploration.ipynb
├── 01_preprocessing_and_splits.ipynb
├── 02_mlp.ipynb
├── 03_cnn.ipynb
├── 04_rnn.ipynb
├── 05_lstm.ipynb
├── 06_gru.ipynb
├── 07_cnn_lstm.ipynb
├── 08_cnn_gru.ipynb
└── 09_model_comparison.ipynb
```

I also currently have these Python files:

```text
__init__.py
config.py
evaluation.py
training.py
utils.py
wesad_preprocessing.py
```

The current Python files already contain useful implementations. Preserve their correct logic where possible, but reorganize and clean them.

## Main objective

Create this package:

```text
wesad_utils/
├── __init__.py
├── config.py
├── helpers.py
├── training.py
├── evaluation.py
└── preprocessing.py
```

Rename:

```text
utils.py → helpers.py
wesad_preprocessing.py → preprocessing.py
```

Update all relative imports accordingly.

The model classes must remain directly inside their respective notebooks because I want to explain the architectures to my professor from the notebooks.

The notebooks should contain:

* Markdown explanations
* data loading calls
* model architecture classes
* model-specific hyperparameters
* loss and optimizer setup
* calls to shared training/evaluation functions
* visualizations
* result interpretation
* artifact-saving calls

The notebooks should not contain duplicated implementations of shared utility functions.

---

# 1. Package structure

Move and update the files to:

```text
wesad_stress_project/
├── wesad_utils/
│   ├── __init__.py
│   ├── config.py
│   ├── helpers.py
│   ├── training.py
│   ├── evaluation.py
│   └── preprocessing.py
├── notebooks/
├── data/
├── artifacts/
├── reports/
├── app.py
├── README.md
└── requirements.txt
```

The package must support imports such as:

```python
from wesad_utils.config import (
    PROJECT_ROOT,
    TRAIN_SUBJECTS,
    VALIDATION_SUBJECTS,
    TEST_SUBJECTS,
    SPLIT_SUBJECTS,
    SEQUENCE_CHANNELS,
    TARGET_HZ,
    WINDOW_SECONDS,
    WINDOW_SAMPLES,
    STRIDE_SECONDS,
    STRIDE_SAMPLES,
    RANDOM_SEED,
)

from wesad_utils.helpers import (
    set_seed,
    count_parameters,
    save_json,
)

from wesad_utils.training import (
    train_one_epoch,
    evaluate_loss,
    train_with_early_stopping,
    pos_weight_from_labels,
    save_model_artifacts,
)

from wesad_utils.evaluation import (
    logits_to_probabilities,
    collect_probabilities,
    select_threshold,
    binary_metrics,
    per_subject_metrics,
    prediction_table,
)

from wesad_utils.preprocessing import (
    run_preprocessing,
    extract_feature_table,
)
```

Use relative imports inside the package:

```python
from .config import ...
from .helpers import ...
```

Do not use fragile imports such as:

```python
from utils import ...
from config import ...
```

---

# 2. Notebook path setup

Add a consistent setup cell near the beginning of every notebook:

```python
from pathlib import Path
import sys

CURRENT_DIRECTORY = Path.cwd().resolve()

if CURRENT_DIRECTORY.name == "notebooks":
    PROJECT_ROOT = CURRENT_DIRECTORY.parent
else:
    PROJECT_ROOT = CURRENT_DIRECTORY

if str(PROJECT_ROOT) not in sys.path:
    sys.path.insert(0, str(PROJECT_ROOT))

print("Project root:", PROJECT_ROOT)
```

After that, import from `wesad_utils`.

Do not duplicate project constants in every notebook. Load shared constants from `wesad_utils.config`.

---

# 3. `helpers.py`

Keep or implement:

```python
def set_seed(seed: int = 42) -> None:
```

It must set:

```python
random.seed(seed)
np.random.seed(seed)
torch.manual_seed(seed)
torch.cuda.manual_seed_all(seed)
torch.backends.cudnn.deterministic = True
torch.backends.cudnn.benchmark = False
```

Also keep:

```python
def count_parameters(model: torch.nn.Module) -> int:
```

and:

```python
def save_json(path: Path, data: dict) -> None:
```

Add recursive conversion of NumPy and PyTorch scalar values when necessary so JSON saving does not fail.

For example, support:

* `np.float32`
* `np.float64`
* `np.int64`
* `torch.Tensor` containing one scalar
* `Path`

Do not silently serialize large tensors into JSON.

---

# 4. `training.py`

Keep the shared PyTorch training functions:

```python
train_one_epoch(...)
evaluate_loss(...)
train_with_early_stopping(...)
pos_weight_from_labels(...)
save_model_artifacts(...)
```

Improve them as follows.

## Correct mean loss

Calculate epoch loss weighted by batch size instead of averaging batch means equally.

Use:

```python
total_loss += loss.item() * batch_size
total_samples += batch_size
epoch_loss = total_loss / total_samples
```

Apply this to both training and validation loss.

## Early stopping

`train_with_early_stopping` must:

* monitor validation loss,
* keep a deep CPU copy of the best `state_dict`,
* restore the best weights before returning,
* store the best epoch,
* store the best validation loss,
* support optional gradient clipping,
* return a structured result.

Prefer returning:

```python
model, history_df, training_summary
```

where `training_summary` contains:

```python
{
    "best_epoch": ...,
    "best_validation_loss": ...,
    "epochs_trained": ...,
    "early_stopped": ...
}
```

Do not return a separate state dictionary unless it is genuinely needed.

## Positive class weighting

Keep:

```python
def pos_weight_from_labels(...)
```

It must calculate:

```text
number of non-stress training windows
-------------------------------------
number of stress training windows
```

Use training labels only.

Raise a clear error if either class is missing.

## Artifact saving

Update:

```python
save_model_artifacts(...)
```

It should save:

```text
best_model.pt
model_config.json
threshold.json
training_history.csv
validation_metrics.json
test_metrics.json
per_subject_metrics.csv
test_predictions.csv
```

Also save:

```text
training_summary.json
```

The threshold file should contain:

```json
{
  "threshold": 0.43,
  "selected_on": "validation",
  "selection_metric": "macro_f1",
  "positive_class": "stress"
}
```

Save models using:

```python
torch.save(model.state_dict(), path)
```

Do not use Joblib for neural-network models.

---

# 5. `evaluation.py`

Keep these functions:

```python
logits_to_probabilities(...)
collect_probabilities(...)
select_threshold(...)
binary_metrics(...)
per_subject_metrics(...)
prediction_table(...)
```

Improve them as follows.

## Input validation

Check that:

```text
len(y_true) == len(probabilities)
```

and that arrays are not empty.

## Threshold selection

Search thresholds from `0.10` to `0.90`.

Use validation macro F1 only.

Define a clear tie-breaking rule. Prefer the threshold closest to `0.5` when two thresholds have the same macro F1.

Return:

```python
best_threshold, threshold_table
```

The threshold table should contain:

```text
threshold
macro_f1
weighted_f1
stress_precision
stress_recall
```

## Binary metrics

Return:

```text
macro_f1
weighted_f1
non_stress_precision
non_stress_recall
stress_precision
stress_recall
roc_auc
average_precision
confusion_matrix
```

Handle ROC-AUC and average precision safely if only one class exists in a per-subject subset.

## Prediction table

Include:

```text
window_id
subject_id
true_label
stress_probability
predicted_label
threshold
```

Validate that metadata length matches the number of predictions.

## Per-subject metrics

Preserve the per-subject evaluation.

Do not allow one participant’s windows to be omitted silently.

---

# 6. `preprocessing.py`

Keep the common preprocessing logic in this module:

* loading WESAD pickle files,
* signal alignment,
* BVP downsampling,
* EDA/TEMP interpolation,
* ACC alignment,
* protocol-label alignment,
* valid-segment detection,
* window creation,
* sequence normalization,
* saving raw and normalized sequence tensors,
* saving metadata,
* statistical feature-extraction functions used by the MLP.

Preserve these decisions:

```text
Target rate: 32 Hz
Window length: 30 seconds
Window samples: 960
Stride: 15 seconds
Stride samples: 480
Channels:
BVP, EDA, TEMP, ACC_x, ACC_y, ACC_z
```

Use:

```python
resample_poly
```

for:

```text
BVP 64 Hz → 32 Hz
```

Use linear interpolation for:

```text
EDA 4 Hz → 32 Hz
TEMP 4 Hz → 32 Hz
```

ACC is already recorded at 32 Hz. Prefer truncation or exact timestamp alignment rather than unnecessary interpolation when possible.

Labels are categorical. Never interpolate labels numerically.

Use nearest-index or interval-mode mapping.

## Data leakage

The preprocessing order must remain:

```text
Assign subjects to split
→ load each subject
→ align each subject
→ create windows inside each subject
→ concatenate by split
→ fit sequence scaler on training windows only
→ transform validation and test windows
```

Never create all windows first and randomly split them.

## MLP feature responsibility

`preprocessing.py` may contain reusable feature-extraction functions:

```python
extract_feature_table(...)
extract_window_features(...)
```

However, `01_preprocessing_and_splits.ipynb` should not fit or save the MLP feature scaler.

The division must be:

```text
01_preprocessing_and_splits.ipynb
→ create raw windows
→ create normalized sequence windows
→ save labels and metadata

02_mlp.ipynb
→ load raw windows
→ extract statistical features
→ remove duplicates/correlated features
→ fit MLP feature scaler using training features only
→ transform validation and test features
→ save MLP feature artifacts
```

Do not save two conflicting versions of:

```text
feature_columns.json
mlp_feature_scaler.joblib
```

## Saved preprocessing artifacts

Use:

```text
artifacts/preprocessing/
├── splits.json
├── sequence_channels.json
├── preprocessing_config.json
├── sequence_scaler.joblib
├── label_mapping.json
└── class_distribution.csv
```

Use Joblib for the `StandardScaler`:

```python
joblib.dump(sequence_scaler, path)
```

Do not save the same scaler redundantly as `.pkl`, `.npz`, and `.joblib` unless there is a documented reason.

Prefer one canonical file:

```text
sequence_scaler.joblib
```

---

# 7. Remove duplicated functions from notebooks

Search all notebooks and remove local definitions of shared functions such as:

```text
set_seed
count_parameters
save_json
train_one_epoch
evaluate_loss
train_with_early_stopping
pos_weight_from_labels
collect_probabilities
select_threshold
binary_metrics
per_subject_metrics
prediction_table
save_model_artifacts
run_preprocessing
```

Replace them with package imports.

Do not remove model architecture classes from their notebooks.

---

# 8. Keep model classes in notebooks

The following classes should remain visible directly in their notebook:

```text
02_mlp.ipynb
→ MLPClassifier

03_cnn.ipynb
→ CNN1DClassifier

04_rnn.ipynb
→ SimpleRNNClassifier

05_lstm.ipynb
→ LSTMClassifier

06_gru.ipynb
→ GRUClassifier

07_cnn_lstm.ipynb
→ CNNLSTMClassifier

08_cnn_gru.ipynb
→ CNNGRUClassifier
```

Each notebook should explain:

* expected input shape,
* architecture layers,
* output shape,
* number of trainable parameters,
* why the architecture is appropriate,
* limitations.

---

# 9. Fair model-variant selection

Each model notebook may compare:

```text
no imbalance correction
class-weighted loss
```

Do not automatically select the class-weighted version.

For each variant:

1. Train using the same random seed.
2. Select its probability threshold using validation data.
3. Calculate validation metrics.
4. Compare validation macro F1.
5. Select the variant with the higher validation macro F1.
6. Evaluate only the selected variant on the test set.

Use code equivalent to:

```python
if weighted_metrics["macro_f1"] >= unweighted_metrics["macro_f1"]:
    selected_model = weighted_model
    selected_method = "class_weight"
    selected_threshold = weighted_threshold
    selected_validation_metrics = weighted_metrics
else:
    selected_model = unweighted_model
    selected_method = "no_correction"
    selected_threshold = unweighted_threshold
    selected_validation_metrics = unweighted_metrics
```

Save the selected method in:

```text
model_config.json
training_summary.json
```

Display a validation comparison table in every model notebook.

Do not use test metrics to choose between variants.

---

# 10. Notebook-specific requirements

## `00_data_exploration.ipynb`

Keep it focused on exploration.

It may use a local loader for educational clarity, but preferably import shared constants.

Do not train models.

## `01_preprocessing_and_splits.ipynb`

The notebook should:

* explain every preprocessing stage,
* call functions from `wesad_utils.preprocessing`,
* show representative intermediate results,
* run leakage checks,
* save raw and normalized windows,
* save metadata,
* not extract or scale final MLP features.

Keep key demonstrations visible, for example:

```python
summary = run_preprocessing(PROJECT_ROOT)
display(summary)
```

Also load and display:

```text
sequence shapes
class counts
subject lists
metadata samples
```

## `02_mlp.ipynb`

The notebook should:

* load `X_train_raw.pt`, `X_validation_raw.pt`, `X_test_raw.pt`,
* extract named statistical features using shared feature functions,
* remove exact duplicate columns,
* remove highly correlated features using training data only,
* fit an MLP scaler on training data only,
* save the scaler using Joblib,
* save feature-column order,
* define the MLP class inside the notebook,
* train and evaluate using shared utilities,
* run SHAP,
* save SHAP figures.

Do not extract MLP features from normalized sequence tensors.

## `03_cnn.ipynb` through `08_cnn_gru.ipynb`

These notebooks should:

* load the normalized sequence tensors,
* define their architecture directly in the notebook,
* use shared training/evaluation functions,
* show one forward-pass shape check,
* compare unweighted versus weighted loss using validation data,
* evaluate the selected model on test data,
* save the common artifact set.

Use gradient clipping for recurrent models and hybrids.

## `09_model_comparison.ipynb`

This notebook must not train models.

It should load all saved model result files and compare:

```text
MLP
CNN
RNN
LSTM
GRU
CNN-LSTM
CNN-GRU
```

Include:

```text
validation macro F1
test macro F1
weighted F1
stress precision
stress recall
ROC-AUC
average precision
parameter count
training time
inference time
selected imbalance method
```

Select the deployment candidate using validation macro F1.

Report test performance only after that selection.

Do not call the model with the highest test score the selected best model.

It may additionally report:

```text
highest observed test macro F1
```

as a descriptive result.

---

# 11. Metadata and consistency checks

In every model notebook, add assertions:

```python
assert len(metadata_train) == len(y_train)
assert len(metadata_validation) == len(y_validation)
assert len(metadata_test) == len(y_test)

assert np.array_equal(
    metadata_test["binary_label"].to_numpy(),
    y_test.numpy().astype(int),
)

assert set(metadata_train["subject_id"]).isdisjoint(
    set(metadata_validation["subject_id"])
)

assert set(metadata_train["subject_id"]).isdisjoint(
    set(metadata_test["subject_id"])
)

assert set(metadata_validation["subject_id"]).isdisjoint(
    set(metadata_test["subject_id"])
)
```

Also verify:

```text
sequence tensor shape = (N, 960, 6)
labels contain only 0 and 1
all inputs are finite
channel order matches sequence_channels.json
```

---

# 12. Artifact formats

Use:

```text
PyTorch models          → .pt
Scikit-learn scalers    → .joblib
Configurations          → .json
Metrics                 → .json
Predictions             → .csv
Training history        → .csv
Metadata                → .csv
```

Do not save neural-network models with Joblib.

Use:

```python
torch.save(model.state_dict(), "best_model.pt")
```

Use:

```python
joblib.dump(scaler, "sequence_scaler.joblib")
```

---

# 13. Explanatory Markdown

The notebooks are used for a professor presentation, so add concise Markdown explanations before important cells.

For example:

```markdown
## Shared training protocol

All neural-network models use the same training and evaluation utilities.
This ensures that early stopping, gradient clipping, validation-threshold
selection, probability conversion, and metric calculation are identical
across architectures. Only the architecture and model-specific
hyperparameters differ.
```

Before model classes, explain tensor shapes.

Before training, explain:

* loss,
* optimizer,
* class weighting,
* early stopping,
* validation threshold selection.

After evaluation, interpret the results and mention the limitation of only 15 subjects.

---

# 14. Final verification

After refactoring:

1. Restart the kernel.
2. Run all notebooks in order.
3. Ensure no notebook depends on stale variables from previous notebook runs.
4. Ensure no duplicate utility definitions remain.
5. Ensure all imports use `wesad_utils`.
6. Ensure all artifacts are regenerated successfully.
7. Ensure notebook 09 includes all seven models.
8. Ensure model selection uses validation results only.

Run order:

```text
00_data_exploration.ipynb
01_preprocessing_and_splits.ipynb
02_mlp.ipynb
03_cnn.ipynb
04_rnn.ipynb
05_lstm.ipynb
06_gru.ipynb
07_cnn_lstm.ipynb
08_cnn_gru.ipynb
09_model_comparison.ipynb
```

## Expected final result

The notebooks should remain educational and easy to present, while shared implementation details are centralized in `wesad_utils`.

The final separation should be:

```text
Notebooks
→ explanations
→ architecture classes
→ experiment configuration
→ training/evaluation calls
→ plots and interpretation

wesad_utils
→ shared constants
→ preprocessing implementation
→ repeated training functions
→ repeated evaluation functions
→ artifact-saving helpers
```

Return all updated Python files and notebooks. Preserve working code and existing useful Markdown, but remove unnecessary duplication and fix inconsistent artifact handling.
